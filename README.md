# backend-rest-api-app
Task 2 is built and tested — Node.js + Express + MongoDB, matching your DevPulse stack.  What's inside:  Users: create, list (paginated), get, update, delete — passwords hashed with bcrypt, never returned in responses Projects: create/retrieve, scoped to an owner, with status (active/archived).
